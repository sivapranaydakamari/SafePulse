import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF00B4D8); // Cyan/Light Blue from SOS Hub
  static const Color secondary = Color(0xFF0077B6);
  static const Color accent = Color(0xFF90E0EF);

  // Safety Status Colors
  static const Color safe = Color(0xFF4CAF50);
  static const Color risk = Color(0xFFE53935);
  static const Color idle = Color(0xFFFFB300);

  // Neutral Colors (Dark Theme Focus)
  static const Color background = Color(0xFF0F172A);
  static const Color cardBg = Color(0xFF1E293B);
  static const Color surface = Color(0xFF334155);
  
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF94A3B8);
  
  // HUD / Map Overlay Colors
  static const Color hudBg = Color(0xCC0F172A);
}
